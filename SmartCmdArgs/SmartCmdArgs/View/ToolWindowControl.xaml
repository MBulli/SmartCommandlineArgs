<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SmartCmdArgs.View"
             xmlns:vm="clr-namespace:SmartCmdArgs.ViewModel"
             xmlns:vs="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.14.0"
             xmlns:c="clr-namespace:SmartCmdArgs.Helper"
             x:Class="SmartCmdArgs.View.ToolWindowControl"
             mc:Ignorable="d"
             Background="{DynamicResource vs:EnvironmentColors.ToolWindowBackgroundBrushKey}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignData ToolWindowViewModel}"
             x:Name="MyToolWindow">
    <UserControl.Resources>
        <BitmapImage x:Key="AddIcon" UriSource="../Resources/iconmonstr-plus-2-icon-32.png"/>
        <BitmapImage x:Key="RemoveIcon" UriSource="../Resources/iconmonstr-minus-2-icon-32.png"/>

        <BitmapImage x:Key="AddIcon2" UriSource="../Resources/list-add.png"/>
        <BitmapImage x:Key="RemoveIcon2" UriSource="../Resources/list-remove.png"/>
        <BitmapImage x:Key="UpIcon" UriSource="../Resources/up-arrow.png"/>
        <BitmapImage x:Key="DownIcon" UriSource="../Resources/down-arrow.png"/>

        <Style x:Key="vsImageButtonStyle" TargetType="vs:ImageButton">
            <!--<Setter Property="BackgroundHover" Value="{DynamicResource {x:Static vs:CommonControlsColors.ButtonHoverBrushKey}}" />-->
            <!--<Setter Property="BackgroundPressed" Value="{DynamicResource {x:Static vs:CommonControlsColors.ButtonPressedBrushKey}}" />-->
            <Setter Property="BackgroundHover" Value="{DynamicResource {x:Static vs:EnvironmentColors.CommandBarHoverBrushKey}}" />
            <Setter Property="BackgroundPressed" Value="{DynamicResource {x:Static vs:EnvironmentColors.CommandBarMouseDownBackgroundGradientBrushKey}}" />
            <Setter Property="ImageHover" Value="{Binding ImageNormal, Mode=OneWay, RelativeSource={RelativeSource Self}}" />
            <Setter Property="ImagePressed" Value="{Binding ImageNormal, Mode=OneWay, RelativeSource={RelativeSource Self}}" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="22" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{DynamicResource {x:Static vs:EnvironmentColors.CommandBarGradientBrushKey}}">
            <vs:ImageButton Style="{StaticResource vsImageButtonStyle}"  ImageNormal="{StaticResource AddIcon2}" Command="{Binding AddEntryCommand}" Width="16" Margin="10, 0, 0, 0"/>
            <vs:ImageButton Style="{StaticResource vsImageButtonStyle}" ImageNormal="{StaticResource RemoveIcon2}" Command="{Binding RemoveEntriesCommand}" Width="16" Margin="10, 0, 0, 0"/>
            <vs:ImageButton Style="{StaticResource vsImageButtonStyle}"  ImageNormal="{StaticResource UpIcon}" Command="{Binding MoveEntriesUpCommand}" Width="16" Margin="20, 0, 0, 0"/>
            <vs:ImageButton Style="{StaticResource vsImageButtonStyle}"  ImageNormal="{StaticResource DownIcon}" Command="{Binding MoveEntriesDownCommand}" Width="16" Margin="10, 0, 0, 0"/>
        </StackPanel>

        <Grid Grid.Row="1" Visibility="{Binding StartupProject, Converter={c:NullToVisibilityConverter Inverted=False}}">
            <local:ListControl x:Name="CmdArgListControl" DataContext="{Binding CurrentArgumentList}" SelectedItems="{Binding SelectedItems, Mode=TwoWay}" MoveUpCommand="{Binding DataContext.MoveEntriesUpCommand, ElementName=MyToolWindow}" MoveDownCommand="{Binding DataContext.MoveEntriesDownCommand, ElementName=MyToolWindow}"/>
        </Grid>

        <Grid Grid.Row="1" Visibility="{Binding StartupProject, Converter={c:NullToVisibilityConverter Inverted=True}}">
            <TextBlock Text="No startup project found." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource {x:Static vs:ThemedDialogColors.ListItemDisabledTextBrushKey}}"  />
        </Grid>

    </Grid>
</UserControl>