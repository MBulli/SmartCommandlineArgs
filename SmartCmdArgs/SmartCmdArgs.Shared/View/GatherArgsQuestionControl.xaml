<UserControl
    x:Class="SmartCmdArgs.View.GatherArgsQuestionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartCmdArgs.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="200"
    d:DesignWidth="400"
    d:Background="White">
    <Grid Margin="8,0,8,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Margin="8" TextWrapping="Wrap">
                The extension is about to gather command
                line args from projects it does not have any
                data for.
                <LineBreak/>
                This solution contains <Run FontWeight="Bold" Text="{Binding ProjectCount, Mode=OneWay}"/>
                <Run FontWeight="Bold" Text="{Binding ProjectKindName, Mode=OneWay}"/> projects.
                This could lead to a long hang and even a out of memory exception.
            </TextBlock>
            <TextBlock Margin="8">
                Do you want to ignore <Run FontWeight="Bold" Text="{Binding ProjectKindName, Mode=OneWay}"/> projects this time?
            </TextBlock>
            <CheckBox x:Name="RememberCheckbox"  Margin="8,1,3,0" Content="Remember for this solution."/>
        </StackPanel>
        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button x:Name="ButtonYes" Width="75" Margin="0,0,10,0" Click="ButtonYes_Click">Yes</Button>
            <Button x:Name="ButtonNo" Width="75" Click="ButtonNo_Click">No</Button>
        </StackPanel>
    </Grid>
    
</UserControl>
